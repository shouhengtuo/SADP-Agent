<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>智能伴学 - 详情</title>
    <!-- Mermaid -->
    <script src="https://cdn.jsdelivr.net/npm/mermaid@10.9.1/dist/mermaid.min.js"></script>
    <script>mermaid.initialize({ startOnLoad: true, theme: 'default' });</script>
    <!-- Coze SDK -->
    <script src="https://lf-cdn.coze.cn/obj/unpkg/flow-platform/chat-app-sdk/1.2.0-beta.19/libs/cn/index.js"></script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', 'Microsoft YaHei', sans-serif;
            display: flex;
            height: 100vh;
            overflow: hidden;
        }
        .panel {
            height: 100%;
            overflow-y: auto;
            padding: 20px;
        }
        .left-panel {
            width: 320px;
            background: #f8fafc;
            border-right: 1px solid #e2e8f0;
        }
        .center-panel {
            flex: 1;
            background: #f1f5f9;
            display: flex;
            align-items: center;
            justify-content: center;
            border-right: 1px solid #e2e8f0;
            overflow: auto;
        }
        .right-panel {
            width: 400px;
            display: flex;
            flex-direction: column;
        }
        .right-header {
            background: #f8fafc;
            padding: 12px;
            text-align: center;
            border-bottom: 1px solid #e2e8f0;
            font-weight: bold;
        }
        .chat-container {
            flex: 1;
        }
        .section-title {
            color: #0f766e;
            margin: 20px 0 10px;
            font-size: 1.4rem;
        }
        .mermaid {
            min-width: 600px;
            min-height: 500px;
        }
        h2, h3 {
            margin: 15px 0 10px;
        }
        p, li {
            margin-bottom: 8px;
            line-height: 1.6;
        }
        ul {
            padding-left: 20px;
        }
    </style>
</head>
<body>
    <!-- 左侧：设计说明 -->
    <div class="panel left-panel">
        <h1>🎓 智能伴学</h1>
        <p>我的智能助教工作流，学习陪伴。</p>
        <h2 class="section-title">1.1 设计目标与核心理念</h2>
        <p><strong>工作流名称</strong>：myIntelligentLearningAssistant</p>
        <p><strong>核心理念</strong>：“授人以渔”而非“授人以鱼”。</p>
        <p><strong>设计目标</strong>：</p>
        <ul>
            <li>支持多模态输入（文字、文档、图片）</li>
            <li>确保专业准确性（RAG + 课程知识库）</li>
            <li>提供双轨制服务（直接答案 / 引导探索）</li>
            <li>实现闭环引导（苏格拉底式多轮问答）</li>
        </ul>
        <h2 class="section-title">1.2 节点级设计</h2>
        <p>包含：输入预处理、RAG检索、意图判断、引导循环等模块……</p>
        <p>（此处可粘贴您文档中的详细表格内容）</p>
    </div>

    <!-- 中间：工作流图 -->
    <div class="panel center-panel">
        <div class="mermaid">
graph TD
    A[开始: 用户输入(question, userName, Doc, Image)] --> B{是否上传Doc?}
    B -- 是 --> C[LinkReaderPlugin: 解析文档/网页]
    B -- 否 --> D{是否上传Image?}
    D -- 是 --> E[OCR插件: 提取图片文字]
    D -- 否 --> F[生成问题: 整合所有输入]
    C --> F
    E --> F
    F --> G[RAG知识库检索]
    G --> H{检索到结果?}
    H -- 是 --> I[大模型润色输出]
    H -- 否 --> J[大模型_自动输出]
    I --> K[回答结果整合]
    J --> K
    K --> L[确认回答模式]
    L -- 直接给答案 --> M[结束]
    L -- 引导解决 --> N[循环引导子流程]
    N --> O[生成引导问题]
    O --> P[问答2: 用户回答]
    P --> Q[判断回答是否达标]
    Q -- 正确 --> R[恭喜! 结束循环]
    Q -- 错误 --> O
    Q -- 想知道答案 --> M
        </div>
    </div>

    <!-- 右侧：嵌入式智能体 -->
    <div class="panel right-panel">
        <div class="right-header">💬 智能伴学 · 实时对话</div>
        <div class="chat-container" id="coze-chat"></div>
    </div>

    <script>
        // 初始化 Coze 智能体
        new CozeWebSDK.WebChatClient({
            config: {
                bot_id: '7567703865425330217', // 智能伴学
            },
            componentProps: {
                title: '智能伴学',
                container: document.getElementById('coze-chat'),
                showTitle: false
            },
            auth: {
                type: 'token',
                token: 'pat_********', // ⚠️ 替换为您的真实 PAT
                onRefreshToken: function () {
                    return 'pat_********';
                }
            }
        });
    </script>
</body>
</html>